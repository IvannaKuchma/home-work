{
  "dependencies": {
    "@reduxjs/toolkit": "^2.8.2",
    "axios": "^1.10.0",
    "react-redux": "^9.2.0",
    "uuid": "^11.1.0"
  },
  "scripts": {
    "json-server": "json-server --watch db.json --port 3001",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0"
  }
}
